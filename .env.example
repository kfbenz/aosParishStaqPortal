# ParishStaq Portal Environment Variables

# Security - generate a random secret key
SECRET_KEY=your-secret-key-here-change-this

# Database - MariaDB connection URL
PORTAL_DATABASE_URL=mysql+pymysql://portal:password@localhost:3306/portal_app

# Google Maps API (for geocoding)
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Optional: Email settings for magic links
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@example.com
